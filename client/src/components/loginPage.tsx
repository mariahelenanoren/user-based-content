import { CSSProperties } from "react";



export default function LoginPage(){
return (
    <div style={mainStyle}>
    </div>
   );
};

const mainStyle: CSSProperties = {   
         display: 'flex',
         flexGrow: 1,
         flexBasis: 0,
    };